# Google Apps Script 運用ガイド

## 日常の更新

1. `admin.html` を開きます。
2. 「本番○件を安全に読み込みました」と表示されたことを確認します。
3. URLを追加・編集・並び替え、またはゴミ箱へ移動します。
4. 必要なら「トップページでプレビュー」を確認します。
5. 「差分を確認して保存」を押します。
6. 追加・変更・ゴミ箱・使用中件数を確認して保存します。
7. 「本番へ保存し、使用中○件を確認しました」と表示されれば完了です。

本番の読み込みに失敗している間は保存できません。接続エラー時に、手元の古いデータで本番を上書きすることはありません。

## 登録項目

- 表示名: スタッフも読める日本語名
- 疾患・業務グループ: 喘息、便秘、アトピー、夜尿症、院内業務など
- 用途: 診察、患者入口、受付、データ確認、院内業務、その他
- 検索キーワード: `enuresis, diary` のような英語・略語・別名
- スタッフ表示: チェックするとスタッフページにも表示。医師ページには常に表示

`enuresis` を登録すれば、`e`、`en`、`enu` の途中入力でも検索できます。途中形を個別登録する必要はありません。

## 削除と復元

「ゴミ箱へ」は即時削除ではありません。管理画面の「ゴミ箱」から復元できます。ゴミ箱の状態も本番へ保存されます。

さらに、GAS保存のたびに保存直前の全データがスプレッドシート内の非表示シート `_portal_backups` へ保存されます。

過去版を戻す場合:

1. スプレッドシートで `_portal_backups` を再表示します。
2. `savedAt` と `activeCount` から対象行を確認します。
3. Apps Scriptに一時的な引数付き関数を作り、対象行を指定します。

```js
function restoreSelectedBackup() {
  return restoreBackup(12);
}
```

4. 実行後、実際の `/exec` URLで件数と内容を確認します。

Google Sheets自体の「ファイル → 変更履歴 → 変更履歴を表示」も併用できます。まず過去版のコピーを作って確認してから復元してください。

## 配布URL

```text
医師用（全サイト）:
https://example.github.io/clinic-web-app-portal/?profile=doctor

スタッフ用（指定サイトのみ）:
https://example.github.io/clinic-web-app-portal/?profile=staff
```

旧 `?profile=reception` もスタッフページとして扱うため、既存QRコードはそのまま利用できます。看護師用と閲覧用管理者プロフィールは廃止します。

## デプロイ

ブラウザ利用のため、必ず **Web app** としてデプロイします。既存デプロイを更新するときは、実行ユーザー、アクセス対象、既存 `/exec` URLが維持されることを確認してください。

デプロイ後は `/exec` URLを直接開き、次を確認します。

- `ok` が `true`
- `apps` が期待件数
- `revision` が存在
- ポータルの医師用・スタッフ用ページが表示される
- 認証境界が意図どおり
