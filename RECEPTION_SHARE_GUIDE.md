# 受付スタッフ用ポータルの共有方法

## 受付に共有するURL

受付スタッフには、次のURLだけを共有します。

```text
https://haman-360.github.io/clinic-web-app-portal/?profile=reception
```

このURLで開くと、登録済みリンクのうち `受付` にチェックを入れたものだけが表示されます。

医師用や管理者用のリンクは、受付スタッフ用URLには表示されません。

## 現在、受付に表示されるリンク

現在の設定では、受付スタッフ用ポータルには次のリンクが表示されます。

- 便秘 QR作成
- パスワード作成

## 受付に見せたくないリンクの扱い

管理画面でURLを登録・編集するとき、`表示するトップページ` で `受付` にチェックを入れなければ、受付スタッフ用ポータルには表示されません。

たとえば、医師だけが使うリンクは `医師` だけにチェックを入れます。

管理者だけが使うリンクは `管理者` だけにチェックを入れます。

## 受付用リンクを追加する手順

1. 管理画面を開きます。

```text
https://haman-360.github.io/clinic-web-app-portal/admin.html
```

2. URL、表示名、説明、カテゴリ、タグを入力します。
3. `表示するトップページ` で `受付` にチェックを入れます。
4. 必要に応じて、`医師` や `管理者` にもチェックを入れます。
5. `追加` または `更新` を押します。
6. `GASへ保存` を押します。
7. 受付スタッフ用URLを開き、表示内容を確認します。

```text
https://haman-360.github.io/clinic-web-app-portal/?profile=reception
```

## 共有前の確認

受付にURLを案内する前に、次の点を確認します。

- 受付スタッフ用URLで、必要なリンクだけが表示されている
- 医師用スプレッドシートや管理者用リンクが表示されていない
- タグで絞り込んでも、受付に見せたくないリンクが出てこない
- 追加・削除直後は、ページを強制再読み込みして確認する

強制再読み込み:

```text
Mac: Command + Shift + R
Windows: Ctrl + F5
```

## 注意点

この仕組みは、ポータル画面での表示を分けるためのものです。

本当にアクセスさせたくないGoogleスプレッドシート、Google Apps Script、Google Driveなどは、リンク先側でも共有権限を制限してください。

受付スタッフに案内する文面の例:

```text
受付用ポータルはこちらです。
https://haman-360.github.io/clinic-web-app-portal/?profile=reception

このページをブックマークして使ってください。
```
